<%= form_for @song  do |f| %>
  <p><%= f.label :title %>
  <%= f.text_field :title %></p>>
  <p><%= artist_select(f, @song) %></p>
  <p><%= f.label :artist_name %>
  <%= f.text_field :artist_name %></p>
  <%= f.submit %>
<% end %>
